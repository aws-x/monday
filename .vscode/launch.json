{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "regexp",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/regexp.js"
        }
    ]
}
